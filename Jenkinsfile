pipeline {
   
   agent any
   
   stages {
       stage('Run docker image') {
           
          steps{
             echo 'Run'
          }
       }

      stage('Build docker image') {
           steps{
             echo 'build'
          }
       }

      stage('Stop docker image') {
           echo 'stop'
       }
   }
  
  
}
